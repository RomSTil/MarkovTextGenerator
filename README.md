# 🧠 MarkovTextGenerator  
### Консольный генератор текста на основе цепей Маркова (DOS-стиль)

MarkovTextGenerator — это учебный консольный проект, который генерирует текст на основе цепей Маркова 1-го или 2-го порядка.  
Проект оформлен в стиле **старых DOS-программ** — с ASCII-баннерами, анимацией, красивым выводом и удобным меню.

Приложение поддерживает:

- Загрузку текстовых файлов для обучения
- Историю последних загруженных файлов (JSON)
- Выбор порядка цепи (1 или 2)
- Генерацию произвольной длины
- Посимвольный вывод (typewriter-эффект)
- Автоматическое сохранение результата в `generated_output.txt`
- Паттерн **Builder** для конфигурации TextGenerator
- Паттерн **Strategy** для переключения моделей Маркова
- Полностью модульную архитектуру

---

## 📁 Структура проекта

/MarkovTextGenerator
│
├── Core/
│ ├── Tokenizer.cs
│ ├── TextPreprocessor.cs
│ ├── MarkovModel.cs
│ ├── IMarkovStrategy.cs
│ ├── MarkovStrategy1.cs
│ ├── MarkovStrategy2.cs
│ └── TextGenerator.cs
│
├── Builder/
│ └── TextGeneratorBuilder.cs
│
├── Infrastructure/
│ ├── FileReader.cs
│ ├── FileWriter.cs
│ └── FileHistory.cs
│
├── UI/
│ └── UserInterface.cs
│
└── Program.cs

---

## 🧱 Архитектура и основные компоненты

### **TextPreprocessor**
Очищает текст:
- нижний регистр  
- удаление лишних символов  
- нормализация пробелов  

### **Tokenizer**
Разбивает текст на последовательность слов.

### **IMarkovStrategy + MarkovStrategy1/2**  
Паттерн **Strategy**.  
Позволяет переключать порядок цепей Маркова:  
- Strategy1 → 1-е слово влияет на следующее  
- Strategy2 → пара слов влияет на следующее

### **MarkovModel**
Хранит таблицу переходов и умеет генерировать текст случайным образом.

### **TextGenerator**
Связывает всё:
- читает файл  
- обрабатывает  
- токенизирует  
- строит модель  
- генерирует итог  

### **TextGeneratorBuilder (паттерн Builder)**  
Конструирует `TextGenerator` из:
- пути файла  
- порядка цепи  
- длины текста  
- предобработчика  
- стратегии  
- токенайзера  

### **FileHistory**
Хранит последние пути к файлам в JSON.  
Позволяет быстро загружать последние датасеты.

### **UserInterface**
Интерактивное DOS-меню:
- загрузка файла  
- выбор параметров  
- генерация  
- вывод текста посимвольно  
- ASCII-баннеры  
- анимации точек  

---

## 🚀 Как запустить

1. Открыть проект в Visual Studio  
2. Убедиться, что установлены NuGet-пакеты:  
   - `Spectre.Console`  
   - `Figgle`  
   - `Newtonsoft.Json`  
3. Собрать проект (`Ctrl+Shift+B`)  
4. Запустить (`Ctrl+F5`)

---

## 💻 Меню приложения

[ Markov GPT ]
Текстовый генератор на основе цепей Маркова
- Загрузить текст
- Настроить параметры генерации
- Запустить генерацию
- Выход


Меню реализовано через **Spectre.Console**.

---

## 🔧 Параметры генерации

- **Порядок цепи**: 1 или 2  
- **Длина текста**: любое число слов  
- **Автосохранение**: `generated_output.txt` в корне  

---

## 📚 История файлов

История хранится в `file_history.json`, пример:

```json
[
  {
    "DisplayName": "RimWorldData",
    "FilePath": "C:/Data/RimWorld.txt"
  }
]
```

🔤 Пример результата
```
лотан сказал что все под контролем но генератор начал дымить
мира кричала что опять все пошло не так
земля под исследовательской комнатой провалилась и появился мегапаук
кира уверяла что надо думать позитивно
```
🧰 Используемые технологии
C# .NET Framework 4.7.2
Spectre.Console — красивый UI в консоли
Figgle — ASCII-баннеры
Newtonsoft.Json — работа с историей файлов
ООП, SOLID
Паттерны: Strategy, Builder

📝 Планируемые фичи
- Настройка скорости посимвольного вывода
- Сохранение нескольких пресетов параметров
- Генерация текста по нескольким файлам сразу
- Дополнительные стратегии (3-го порядка)
- Поддержка UTF-символов и эмодзи

👨‍💻 Автор

Проект разработан пользователем **RomSTil**
для изучения алгоритмов и архитектурных паттернов в C#.
